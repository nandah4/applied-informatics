<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /applied-informatics/

    #Redirect semua request ke folder public/
    RewriteCond %{URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
